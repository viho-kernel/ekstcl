apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop
  region: us-east-1

managedNodeGroups:
  - name: spot
    instanceTypes:
      - t3.large
      - t3a.large
      - m5.large
      - m5a.large
      - c5.large
    spot: true
    desiredCapacity: 2
